import { OutputLibrary } from "~/components/outputs"

/** Selects the full dashboard presentation of the reusable output library. */
export const OutputLibraryPage = () => <OutputLibrary source="dashboard" />
