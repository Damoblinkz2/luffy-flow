import { PromptHistory } from "~/components/prompts"

/** Exposes prompt history as a route-level page while keeping its behavior reusable. */
export const PromptHistoryPage = () => <PromptHistory />
